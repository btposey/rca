# Pod deployment variables — sourced by deploy scripts
# MODEL_HOST_PATH defaults to $PROJECT_ROOT/models if not set here.
# POSTGRES_DATA_PATH defaults to $PROJECT_ROOT/data/postgres if not set here.
POD_NAME=rca-pod
API_PORT=8080
VLLM_DISPATCHER_PORT=8000
VLLM_CONCIERGE_PORT=8001
POSTGRES_PORT=5432
